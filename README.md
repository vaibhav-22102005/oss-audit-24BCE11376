# 



Vaibhav Saini (24BCE11376)

Software Chosen: Mozilla Firefox



Scripts

Script 1

This script displays basic information about the Linux system. It shows the distribution name, kernel version, current logged-in user, system uptime, date, and license information. It uses variables, command substitution, and echo statements to format the output in a readable manner.



Run Using chmod +x script1.sh ./script1.sh



Script 2

This script checks whether the chosen open-source software (Firefox) is installed on the system. It retrieves package details using the Debian package manager and prints a short description using a case statement. The script demonstrates the use of conditional statements and package inspection commands.



Run Using chmod +x script2.sh ./script2.sh



Script 3

This script audits important system directories and displays their permissions, owner, group, and disk usage. It loops through a predefined list of directories and extracts information using Linux commands. It also checks whether the Firefox configuration directory exists and prints its permissions.



Run Using chmod +x script3.sh ./script3.sh



Script 4

This script analyzes a log file and counts the number of occurrences of a specified keyword such as “error.” It reads the file line by line using a while loop and increments a counter when matches are found. It also prints the last five matching lines from the log file.



Run Using chmod +x script4.sh ./script4.sh /var/log/syslog



Script 5

This script interactively asks the user three questions related to open source. Based on the responses, it generates a personalized open-source manifesto and saves it to a text file. It demonstrates user input, string handling, file creation, and date commands.



Run Using chmod +x script5.sh ./script5.sh

